replicaCount: 2


# Simple image string that GitHub Action will replace
image: 251374076386.dkr.ecr.us-east-1.amazonaws.com/thegriffinfamily-link-resume-repo:d5de827d66cb65d5b064fdb4ae73eea0d6073ca6

service:
  type: ClusterIP
  port: 80

ingress:
  enabled: true
  annotations:
    kubernetes.io/ingress.class: alb
    alb.ingress.kubernetes.io/scheme: internet-facing
    # Keep basic annotations here that don't change between environments; other annotations will be passed in at runtime through the application.yml workflow
  hosts:
    - host: www.thegriffinfamily.link
      paths:
        - path: /resumes/mike
          pathType: ImplementationSpecific
  tls:
    - hosts:
        - www.thegriffinfamily.link

resources: {}
nodeSelector: {}
tolerations: []
affinity: {}